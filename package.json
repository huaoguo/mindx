{
  "name": "mindx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:skill": "node scripts/build-skill.js",
    "build:web": "cd web && npm install && npm run build",
    "prestart": "npm run build:skill && npm run build:web",
    "start": "node server.js",
    "predev": "npm run build:skill && npm run build:web",
    "dev": "nodemon --watch server.js --watch db.js --watch skill server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
